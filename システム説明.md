# システム概要

ユーザーがオリジナルスキルを作成し、ランダムに出現する敵モブと戦闘を行うターン制RPGシステム。

プレイヤーはログイン後、自身でスキルを作成できる。  
スキルには攻撃・防御・回復の３種類があり、AP（アクションポイント）を消費して戦闘中に使用する。

戦闘ではランダムに敵モブが出現し、プレイヤーは作成したスキルを使用して敵を倒す。  
また、敵モブも攻撃・防御・回復の行動をランダムで実行する。

REST API の GET / POST / PUT / DELETE を使用してデータ管理を行っている。

---

# システム説明

## ログイン機能

ユーザー登録およびログイン機能を実装している。

ログイン成功時にはトークンを発行し、ログイン状態を保持する。  
useAuth を利用してログイン確認を行い、未ログイン状態ではログイン画面へ遷移する。

---

## スキル作成機能

ユーザーはオリジナルスキルを作成できる。

### 作成可能なスキル種類

- attack（攻撃）
- defend（防御）
- heal（回復）

各スキルには以下の情報を持たせている。

| 項目 | 内容 |
|---|---|
| title | スキル名 |
| type | スキル種類 |
| cost | AP消費量 |
| power | 威力 |
| description | スキル説明 |
| totalCost | スキル総コスト |

スキルにはコスト制限を導入しており、  
威力や効果に応じて totalCost が増加する。

上限値を超えた場合は作成できない。

---

## 敵モブ機能

管理者ユーザーのみ敵モブを作成できる。

敵モブには以下の情報を持たせている。

| 項目 | 内容 |
|---|---|
| name | 敵名 |
| hp | HP |
| image | 画像 |
| actions | 行動一覧 |

敵の actions には複数の行動パターンを登録可能である。

例：

```json
[
  {
    "actionType": "attack",
    "power": 20
  },
  {
    "actionType": "heal",
    "power": 15
  }
]
```

---

# バトル機能

## バトルシステム概要

戦闘はターン制で進行する。

プレイヤーターンでは、所持しているスキルの中からランダムに5個選出され、その中からスキルを使用する。

敵を倒すと次ステージへ進行し、プレイヤーHPが0になると敗北となる。

---

## ターンの流れ

戦闘は以下の順番で進行する。

1. プレイヤーターン開始
2. スキル使用
3. ターン終了
4. 敵ターン開始
5. 敵行動実行
6. 次ターンへ

---

## AP（アクションポイント）

プレイヤーは AP を消費してスキルを使用する。

| 項目 | 内容 |
|---|---|
| 最大AP | 5 |
| 消費タイミング | スキル使用時 |
| 回復タイミング | 敵ターン終了後 |

AP が不足している場合はスキルを使用できない。

また、同じスキルは1ターン中に1回のみ使用可能である。

---

## スキル使用機能

プレイヤーはスキルを使用して戦闘を行う。

### attack（攻撃）

敵にダメージを与える。

敵にシールドが存在する場合：

- シールド値がダメージ以上  
  → HP ダメージ無し  
  → シールドのみ減少

- ダメージがシールド値を超える  
  → シールド破壊  
  → 余剰ダメージを HP に与える

---

### defend（防御）

シールドを展開する。

シールドはダメージを吸収する役割を持つ。

また、防御スキルは重ね掛け可能となっており、  
複数回使用するとシールド値が加算される。

例：

```text
シールド10状態で
防御20を使用

→ シールド30
```

シールドはターン終了時に消滅する。

---

### heal（回復）

プレイヤーHPを回復する。

HPは最大値を超えないよう制御している。

---

# 敵モブ戦闘機能

## 敵行動

敵モブは actions に登録された行動をランダムに実行する。

行動種類：

- attack
- defend
- heal

---

## 行動予測機能

敵は次ターンで実行予定の行動を事前表示する。

画面上では以下を表示する。

- 行動タイプ
- 威力

例：

```text
次の行動
攻撃⚔️
20
```

予測された行動は実際の敵行動でも使用されるため、  
プレイヤーは事前に対策可能である。

---

## シールドシステム

プレイヤー・敵の両方にシールド機能を実装している。

シールドが存在する場合：

1. まずシールドへダメージ適用
2. シールド超過分のみHPへ適用

例：

```text
敵シールド：30
攻撃力：20

→ シールド10残る
→ HPダメージ0
```

```text
敵シールド：10
攻撃力：25

→ シールド破壊
→ HPへ15ダメージ
```

---

## 戦闘ログ機能

戦闘中の行動履歴をログとして表示する。

表示内容：

- スキル使用
- ダメージ
- 回復
- シールド展開
- ターン開始

最新ログが上に表示される。

---

## ステージ機能

敵を倒すと自動で次ステージへ移動する。

```text
ステージ1 → ステージ2 → ステージ3
```

ステージごとに出現する敵モブが異なる。

---

## UI構成

バトル画面では RPG風UI を採用している。

### 画面構成

| 位置 | 内容 |
|---|---|
| 左上 | 敵ステータス |
| 左下 | プレイヤーステータス |
| 中央 | 敵画像 |
| 右側 | 戦闘ログ |
| 下部 | スキル一覧 |

---